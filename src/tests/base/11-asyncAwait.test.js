import getImage from '../../base/11-asyncAwait';

describe ('Pruenas con async-await Fetch', () => {

  test('Dede de retornar el url de la imagen', async () => {
  /*   console.log(url);
    const url = await getImage(); */

/* 
    expect(typeof url).toBe('string'); */
  });
});